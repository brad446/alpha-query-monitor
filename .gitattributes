# Git LFS Configuration for PDF Files
# This ensures large PDF files are handled efficiently by Git LFS

# PDF files
*.pdf filter=lfs diff=lfs merge=lfs -text

# Large document formats
*.docx filter=lfs diff=lfs merge=lfs -text
*.xlsx filter=lfs diff=lfs merge=lfs -text
*.pptx filter=lfs diff=lfs merge=lfs -text

# Archive files (if needed)
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text

# Large video/media files (optional)
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text

# Keep text files in git for easier diffing
*.json -filter -diff -merge
*.md -filter -diff -merge